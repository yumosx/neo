let a = 2;
a = 1;

fn add(a : int, b : int) int {
    if (a > 0) {
        result a + b;
    } else {
        result b - a;
    }
}

let arr = [1, 2, 3, 4];
let [a, b, c, d] = arr;

type A record {
    name : string;
    age : int;
}

let a = A{name: 1, age : 2}

fn get(a : A) string {
    result A.name;
}

A.get();
